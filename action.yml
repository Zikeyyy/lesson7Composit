name: composite
description: calculate to test composite actions
inputs:
  number1:
    required: true
    defaults: 0
  number2:
    required: true
    defaults: 0
outputs:
  results:
    value: ${{ steps.<>.outputs.<> }}
runs:
  using: composite
  steps:
    - shell: bash
    id: sum-result
    run: |
      num1=${{ inputs.number1 }}   
      num2=${{ inputs.number2 }}
      results=$((num1+num2))
      echo "result=$result" >> "$GITHUB_OUTPUT"
